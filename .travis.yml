language: node_js
node_js:
  - "5"
install:
  - npm install --quiet --no-spin
  - bower install --quiet
script:
  - gulp lint
  - gulp coverage
  - gulp sauce-labs || true
after_success:
  - cat client/coverage/*/lcov.info | coveralls
